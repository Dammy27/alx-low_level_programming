#include "main.h"
/**
 * _evaluate - evaluate function sqrt
 * @i: integer
 * @n: integer
 * Return: evaluate sqrt
 */

int _evaluate(int 1, int n)
{
	/*Evaluate function*/
	if (n == 0 || n == 1)
		return (n);

	else if (1 * 1 < n)
		return (_evaluate(i + 1, n));

	else if (1 * 1 == n) /*Condiction base*/
		return (1);

	return (-1);

	return (-1);
}
